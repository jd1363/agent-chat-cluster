# Agent Chat 项目记忆库

## 2026-06-22

### Block 3A-3F 全部完成，阶段 3 窟窿补齐

- **Block 3A**：`ORIGINAL_PLAN_GAP_ANALYSIS.md` — 原始方案 66 项功能逐条对照，58% 已完成、23% 部分完成、12% 未完成、5% 暂缓、3% 禁止。
- **Block 3B**：`scripts/check_alerts.py` — 多维度只读告警检查器，7 个维度（failed 任务/超时/Agent 状态/审计异常/未 ACK 消息/成本超预算/日志体积），不自动修复，退出码 0/1/2。
  - 修复了 Windows GBK 编码问题：emoji 改用 ASCII tag（[INFO]/[WARN]/[CRIT]），stdout 用 buffer.write utf-8。
- **Block 3C**：`ACCEPTANCE_CONFIG_SNAPSHOT.md` — 配置快照验收通过（save/list/show/restore 全链路）。
- **Block 3D**：`ACCEPTANCE_COST_LEDGER.md` — 成本台账验收通过（record/show/by-agent/by-task/budget 全链路）。
- **Block 3E**：`ACCEPTANCE_COMMAND_MAP.md` + `docs/COMMAND_REFERENCE.md` — 命令映射验收 + 完整命令参考文档。
- **Block 3F**：标签/别名/定时任务说明已写入 `docs/COMMAND_REFERENCE.md`。
- 清理了历史临时文件（test_mimo.txt、test_mimo2.txt、.codex-temp-prompt.txt）。
- 回归验证全通过：py_compile / check_env / validate_task / check_alerts。
- 提交：`a28f7ef feat: complete Block 3A-3F stage 3 gap closure`。

### 阶段 4：7 Agent 扩容 + 并发压力测试 ✅

- **ext03~ext06 启用**：config/agents.json 中 4 个 Agent 从 false 改为 true，7 个 Agent 全部就位。
- **环境隔离校验**：test_isolation.py 通过，7 个 Agent cwd 互不重叠。
- **7 Agent 并发压力测试**：Task-022~028 同时派发，全部 done。
  - Task-022 (agent-exec-01): 统计 scripts 目录 .py 文件 → 30 个 ✅
  - Task-023 (agent-ext-01): 统计 docs 目录 .md 文件 → 6 个 ✅
  - Task-024 (agent-ext-02): 列出 config 目录文件 → 3 个 ✅
  - Task-025 (agent-ext-03): 统计审计日志条目 → 20 条 ✅
  - Task-026 (agent-ext-04): 列出派工日志 → 19 个 ✅
  - Task-027 (agent-ext-05): 统计记忆文件 → 3 个 ✅
  - Task-028 (agent-ext-06): 列出验收文档 → 10 个 ✅
- **提交**：`40b8671` + `91d595b`
- **结论**：7 Agent 并发调度全链路通过，dispatch→execute→collect→done 闭环稳定。

### 并发安全修复 ✅

- **问题**：tasks.json 无文件锁，7 Agent 并发写入导致 JSON 损坏 / Task ID 重复 / 任务丢失
- **修复**：新建 `scripts/file_lock.py` 跨平台文件锁（Windows msvcrt + Unix fcntl）
- **加锁脚本**：create_task / update_task / complete_task / dispatch_task / openclaw_executor
  - read-modify-write 全部包裹在排他锁内
  - 读操作用共享锁
  - 审计日志在锁外执行，减少锁持有时间
- **验证**：py_compile / check_env / validate_task 全通过，Task-029/030 nextId 正确递增
- **提交**：`9fd769e fix: add cross-platform file lock to tasks.json read-modify-write in 5 scripts`

### 当前状态

- 30 个任务：24 done, 3 failed, 3 cancelled
- 7 个 enabled Agent：exec01, ext01~ext06
- git 提交链完整，工作区干净

### Web Dashboard 完成 ✅

- **`web/dashboard.html`** — 暗色主题单页应用，6 大模块：
  1. 任务总览：30 个任务卡片墙，按状态分色，支持 Agent 筛选
  2. Agent 状态：8 个 Agent 卡片，enabled 绿点/disabled 灰点
  3. 审计日志：时间线 + eventType 统计
  4. 消息总线：表格展示，未 ACK 标红
  5. 成本台账：总成本大数字 + 按 Agent 进度条
  6. 告警概览：红/黄/绿分级，failed/unacked/disabled/pending
- **`web/server.py`** — Python 标准库 HTTP 服务，端口 8765
  - 7 个 API 路由：/api/tasks /api/agents /api/audit /api/messages /api/cost /api/alerts /
  - 修复了 CORS header 顺序导致 HTTP 协议冲突的 bug
  - 30 秒自动刷新
- **全 API 测试通过**：tasks(30) agents(8) audit(5) messages(5) cost alerts HTML
- **缺口分析更新**：#53 #54 从「⚠️ 部分完成」改为「✅ 已完成」
- **提交**：`6238a71 feat: web dashboard with 6 modules`
- **启动方式**：`python web/server.py` → 浏览器打开 `http://localhost:8765`

### Web Dashboard 完成 ✅

- 交付物：`web/dashboard.html` + `web/server.py`
- 6 大模块：任务总览（卡片墙+状态分列）/ Agent 状态（启停指示灯）/ 审计日志（时间线+类型统计）/ 消息总线（ACK 状态表格）/ 成本台账（总成本+Agent 分布进度条）/ 告警概览（红黄绿分级）
- 技术方案：纯 HTML+CSS+JS 零依赖前端 + Python 标准库 http.server 后端
- 启动方式：`python web/server.py --port 8765`，浏览器访问 `http://localhost:8765`
- 修复了 CORS header 顺序导致 HTTP 协议冲突的 bug
- 全部 6 个 API 端点测试通过
- 更新 `ORIGINAL_PLAN_GAP_ANALYSIS.md` #53 #54 → ✅ 已完成
- 提交：`6238a71` + `d1b1c32`

### 烂尾清理（20:00）✅

主人要求把烂尾挨个处理。体检发现 7 类问题，逐项清理：

1. **4 个 pending 烂尾任务 → cancelled**
   - Task-029/030（锁测试，从未派发）
   - Task-038/039（D1 event 测试，从未派发）
2. **3 个 failed 任务 → cancelled**
   - Task-005/007（阶段3旧失败，脚本实际已验证通过）
   - Task-013（Hermes e2e 测试，Hermes 已禁用）
3. **37 条未 ACK 消息 → expired**
   - 全是回归测试产生的 sent 状态消息，无业务意义
   - 写临时脚本批量标为 expired，check_alerts 从 critical 降到 info
n4. **list_tasks.py 编码 bug** → 派子 Agent 修复中
5. **PROJECT_STATUS.md / GAP_ANALYSIS / MVP_DELIVERY_REPORT 过期** → 派子 Agent 更新中

清理后任务台账：29 done, 10 cancelled, 0 pending, 0 failed
告警级别：critical → info（只剩 hermes 禁用和历史审计记录的 INFO）

### 烂尾清理完成（20:01）✅

- list_tasks.py 编码 bug 修复（子 Agent 完成）：stdout reconfigure UTF-8
- PROJECT_STATUS.md / MVP_DELIVERY_REPORT.md / GAP_ANALYSIS.md 全部更新到最新（Phase 0-4 ✅）
- 提交：`07895a6` + `8f4331f`
- 最终状态：39 任务（29 done, 0 pending, 0 failed, 10 cancelled）

### Web Dashboard v2：SSE 实时推送 + 操作面板 + 日志流（20:39）

主人要求做前端可视化，ABC 全要：
- A. 实时监控（SSE 推送任务/Agent/审计变化）
- B. 可操作面板（创建/派发/完成/取消任务、发消息）
- C. 实时执行日志流（终端风格）

进度：
- 后端 `server.py` 重写完成（623 行）：POST API + SSE 流 + CORS + OPTIONS ✅
  - POST /api/tasks/create|dispatch|complete|cancel
  - POST /api/messages/send
  - SSE /api/stream/events（任务/Agent/审计实时推送）
  - SSE /api/stream/tasks/{id}/logs（执行日志实时推送）
  - 全部 API 测试通过
- 前端 `dashboard.html` 重写中（子 Agent 正在执行）
  - 第一版子 Agent 写的前端缺 B/C 功能，只有美化
  - 第二版子 Agent 已派出，要求三栏布局+SSE+操作面板+日志流

### 真实执行引擎接入（21:13）— 明天干

主人要求把 Codex/CodeWhale/OpenCode 等真实编码 Agent 接入系统。

系统已安装的 CLI：
- **Codex** (OpenAI Codex CLI) v0.140.0 → `codex exec "prompt"`
- **CodeWhale** v0.8.53 → `codewhale exec "prompt"`
- **OpenCode** v1.17.9 → `opencode run "message"`
- **Ollama** 本地模型（Qwen2.5-14B 等）→ `ollama run model "prompt"`
- MiMo Code 未安装

计划映射：
| Agent ID | 显示名 | 执行引擎 | 非交互命令 |
|---|---|---|---|
| agent-exec-01 | Codex | codex exec | `codex exec "prompt"` |
| agent-ext-01 | CodeWhale | codewhale exec | `codewhale exec "prompt"` |
| agent-ext-02 | OpenCode | opencode run | `opencode run "message"` |
| agent-ext-03 | Ollama | ollama run | `ollama run qwen2.5:14b "prompt"` |
| agent-ext-04 | Codex-2 | codex exec | `codex exec "prompt"` |
| agent-ext-05 | CodeWhale-2 | codewhale exec | `codewhale exec "prompt"` |
| agent-ext-06 | OpenCode-2 | opencode run | `opencode run "message"` |

明天要做：
1. 更新 `config/agents.json`：给每个 Agent 加 name + executor 字段
2. 写 `scripts/executor_bridge.py`：把 dispatch_task 的派工转发到对应 CLI
3. 前端显示真实名字（ID→name 映射）
4. 测试真实执行

### 当前 Web 服务器
- `python web/server.py --port 8765` 正在运行
- 旧版 dashboard.html 仍在（子 Agent 重写完成后替换）
