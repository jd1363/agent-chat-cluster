# 2026-07-20 Agent Chat 集群 - 升级线启动

## 决策：启动系统化升级线（方案 A）
- 主人从"没做完的事"里选定继续做 Agent Chat 集群，方向选 A（系统化升级 Milestone D+）
- 论文写作技能套件已**枪毙**（`C:\Users\jjd\Desktop\paper skill\` 目录已不存在，主人说不要了）

## 关键真相：PROJECT_STATUS 虚账
- 文档说 Milestone A/B/C "骨架已实现"，实际：
  - `event_log.py` ✅ 真在（15KB）
  - `build_state.py` ❌ 6-30 已删（498行，dead script）
  - `scheduler_tick.py` ❌ 6-30 已删（441行，dead script）
- **但这是好事**：项目已迁 SQLite（`data/cluster.db` 396KB + `db.py` 统一层，六表 tasks/audit/cost/events/messages/meta）。Milestone B 目标其实达成了，只是实现方式从独立脚本升级成 SQLite+db.py，旧脚本过时被清理，文档没同步 → 虚账。
- **真正缺的**：真实调度器 scheduler_tick.py（现有 queue_dispatch.py 是手动批处理且仍读旧 tasks.json）

## OpenSpec 规格已完成（用 claude code 起草）
- 位置：`openspec/changes/add-scheduler-tick/`
- `openspec init . --tools claude` 初始化（装了 .claude/ 6 skills+6 commands）
- claude code headless 起草，产出 4 artifact 全部 `openspec validate --strict` = valid：
  - proposal.md / design.md(5.4KB) / specs/scheduler-tick/spec.md(6.6KB, 9 Requirement+24 场景) / tasks.md(2.7KB, 7组21任务)
- **行为契约**：读 db pending 按 high>medium>low+created 排序 / 查 in_progress 做背压(concurrency_cap) / 调 executor_bridge 真实执行回写 SQLite / --dry-run(默认)/--once/--loop N / 三点审计留痕 / --execute-real 门禁 / 无广播自愈外发

## ⚠️ claude code headless 调用的坑（重要）
1. `--permission-mode acceptEdits` 只放行文件编辑，**不放行 Bash** → 卡审批。要全自动用 `bypassPermissions`
2. slash command `/opsx:propose` 在 `-p` headless 下会 Skill 中断，改用**直白 prompt 描述 openspec CLI 三步流程**更稳
3. **写文件乱码**：claude 输出 UTF-8 但落盘按 GBK → 破折号等非 ASCII 变乱码。解法：prompt 里强制"用 ASCII/英文、普通连字符-"
4. 中转站 yuesecoding.top：claude 端点能用（充值后 spec 全出），但会 524 超时（origin timeout）
5. 成本：Opus 起草这套 spec 约烧 $1.54（含调试）

## 新增 2 个执行 Agent（config/agents.json 7→9）
- **agent-ext-07 = Reasonix**（DeepSeek 原生）：`reasonix run {prompt} --effort high`，实测连通成本仅 claude 5%，timeout 300s
- **agent-ext-08 = Claude Code**：`claude -p {prompt} --permission-mode bypassPermissions`，timeout 600s
- executor_bridge 是**配置驱动**，无硬编码 CLI 白名单，自动解析 .ps1，加 config 即可，dry-run 验证两者命令拼接正确
- 备份：`config/agents.json.bak-add-agents-*`

## 🎯 分工定调（主人明确）
- **claude code = 智囊**：出方案、订计划，**不下场干活**
- **reasonix = 主力执行**：下场写代码
- **codex = 搁置**：二进制已重装修好（codex-cli 0.144.6），但中转站 yuesecoding.top 的 `/v1/responses` 端点被 Cloudflare 403 拦（人机验证页），非我方能修。主人说"不差他了"。等中转站通了随时能启用（两个 codex Agent agent-exec-01/agent-ext-05 保持禁用）

## 小 bug 待修
- `C:\Users\jjd\.codex\skills\cyber-ppt\SKILL.md` 缺 YAML frontmatter，codex 每次启动报错（不影响主流程）

## 下一步
- 用 reasonix 下场实现 scheduler_tick.py（照 tasks.md 21 个任务），claude code 需要时出方案

## ✅ scheduler_tick.py 已实现并验收（reasonix 下场）
- **分工兑现**：reasonix 下场写代码（成本 $0.0014，省 vs claude 98%），胖小拆brief+调度+验收
- **产出**：`scripts/scheduler_tick.py`（251行），OpenSpec 21个任务全实现；queue_dispatch.py 加弃用注释
- **brief 落盘**：`.briefs/scheduler_tick_brief.txt`（喂精确接口签名 db.py/executor_bridge.py，防瞎编API）
- **验收通过**：py_compile OK；--help 四flag全在；dry-run 打真库：pending 4→4、in_progress 0→0（状态零改动），audit 510→512（tick_start+tick_done 两点落库），tick_done 带统计
- **踩坑**：Tee-Object 默认存 UTF-16 → 10063 个 null 字节导致 SyntaxError；reasonix em-dash 落盘乱码。解法：从 raw 按 utf-16 解码重提取 + 强制 ASCII 化。避坑手册应验。
- **commit**: 65d184e
- **待验**：7.2 --once --execute-real 真跑（需给 pending 任务配 assignee，当前4个都无 assignee 被安全跳过）

## ✅ 7.2 端到端真跑验证通过
- 新建 Task-086（assignee=agent-ext-07/reasonix），`--once --execute-real` 真跑：**pending→completed**，reasonix 产出 `def hello(): return "hello"`，成本 $0.000052，状态写入 SQLite
- **过程修了3个 bug**（reasonix 初版的接口理解偏差）：
  1. `command_template + substituted_args` — str+list TypeError
  2. `result.get('returnCode')` — execute_cli 实际返回 key 是 `success`(bool)/`output`/`error`/`quality`，无 returnCode → 永远判 failed
  3. `executor_bridge.update_task_status` 写的是旧 **tasks.json 不是 SQLite**（数据源分裂老坑残留）→ 改用 `db.update_task`
  4. 手搓命令拼接绕过了 `executor_bridge.build_command_args()` → reasonix 是 .ps1，shell=False 找不到 `命令未找到: reasonix` → 改用现成的 build_command_args（内置 _resolve_windows_command 处理 .ps1/.cmd）
- **教训**：派子 Agent 写代码 brief 里就算喂了接口签名，它仍可能对现有工具函数的**返回结构**和**该不该复用现成 helper** 理解偏差。验收必须真跑，不能只 py_compile。
- commit: 65d184e(初版) + 接口修复 commit
- **下一步选项**：① 接 Dashboard --loop 常驻调度 ② openspec archive add-scheduler-tick 归档

## ✅ Dashboard 前端重做 + 调度器控制面板（commit c108157）
- **装了 superdesign skill**（ClawHub `superdesign@1.0.0`，不是 @mpociot/superdesign 那个 slug——git/npm 写法装不了，ClawHub 有对应）。本质是前端设计规范（oklch 配色/Inter+JetBrains Mono/lucide 图标/微动效/无障碍）
- **reasonix 重做的坑**：出了个 40KB 漂亮壳子（oklch/lucide/tailwind 都到位），但**接线全编**：自创 REST 风 API（DELETE /api/tasks/id、POST /api/tasks/id/retry）、漏整个 SSE、漏调度器面板。跟 scheduler_tick 一样的毛病——**对现有接口自作主张**。
- **处理**：壳子质量够好保留，胖小亲自精修接线（这是我最了解真实后端的部分）：修 create/execute/kill/rerun 对齐真端点、加 EventSource SSE、加调度器面板、加 description 字段、清 261 个乱码
- **后端 server.py 新增 3 端点**（之前被中断那次没做完）：`/api/scheduler/start`（--loop N，默认 dry-run，单例防重）/`stop`（terminate+kill）/`status`（GET），用 `_SCHEDULER_PROC` 全局 + 现有 Popen 模板
- **真启动实测通过**（端口 8765）：dashboard 200、status→start→running→拒绝重复→stop→stopped 全闭环
- **小 bug 修**：stop 不带 body 被 `_read_json_body` 拒（empty body）→ 前端 stopScheduler 改成带 `{}`
- 备份：旧版 dashboard_backup_0713.html、reasonix 原始壳子 dashboard_redesign_raw.html
