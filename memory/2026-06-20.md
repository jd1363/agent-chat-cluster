# Agent Chat 项目记忆库

## 2026-06-20
- 接着阶段3消息总线收尾工作。
- 修复 `scripts/receive_message.py` 的 Task-009 review 问题：
  - `mark_read()` 不再覆盖原始 `timestamp`，改写 `readAt`。
  - 读取消息日志前增加单文件大小上限 `MAX_MESSAGE_LOG_BYTES = 5MB`。
  - OSError 报错不再直接暴露完整路径，只输出安全错误摘要。
  - 类型标注从 `list[dict]` / `dict | None` 改为 `typing.List/Dict/Optional`，兼容 Python 3.8/3.9。
- 回归验证通过：`py_compile`、`check_env.py`、`validate_task.py`、`receive_message.py --mark-read --json`。

## 2026-06-20 11:56
- 清理历史遗留任务台账：
  - Task-001 从 in_progress 标记 done：阶段2第三块 policies 读取验证已完成，输出指向 `ACCEPTANCE_STAGE2_PRECHECK_BLOCK3.md`。
  - Task-002 从 pending 管理员 force 标记 done：Block3 code review 已完成并记录在 `ACCEPTANCE_STAGE2_PRECHECK_BLOCK3.md`。
- 回归验证：`validate_task.py` 通过；`list_tasks.py --status pending` 和 `--status in_progress` 均为空。

## 2026-06-20 12:02
- 阶段3消息总线策略门禁收口：
  - `send_message.py --to all` 与 `broadcast.py` 现在读取 `config/policies.json`；当 `globalBroadcast.allowed=false` 时必须显式 `--manual-approval`，否则拒绝执行。
  - 多播审计 `broadcast_sent` 增加 `manualApproval` 字段。
  - 文档同步更新：README、OPERATOR_RUNBOOK、SECURITY_NOTES 明确“受控主控多播 ≠ Agent 群聊”。
- 回归中发现并修复消息 ID 并发分配 bug：并行发送曾导致两个进程同时拿到 `MSG-0008`。
  - `send_message.py` 为 `logs/messages/.state` 增加 `.state.lock` 跨进程锁。
  - 并发回归验证：并行发送得到 `MSG-0010` / `MSG-0011`，无撞号。
- 处理 Windows PowerShell BOM 坑：被 PowerShell 写过的 JSON/JSONL 转回 UTF-8 无 BOM。

## 2026-06-20 14:03
- 主人提出 Agent Chat 从 MVP 升级到真正系统的关键判断，已写入 `PROJECT_PLAN.md` 的“从 MVP 到系统级架构升级”章节。
- 关键判断：项目已是“可运行的受控多智能体系统原型”，但仍处于“工程化 MVP 完成 → 系统化架构缺失”的临界点；能跑，但还不是完整系统。
- 当前核心缺口：
  1. 缺少真正 Scheduler：优先级调度、资源分配、并发控制、backpressure、自动选 agent、自动重试、负载均衡。
  2. 缺少 Event Bus/Event Layer：应从 command→script→result 升级为 event→queue→handler→state update，支持 event store、event replay、async dispatch。
  3. Agent 仍非自治：当前更像 worker，不自主决策/规划/选工具。
  4. 缺少统一 State Store：需要统一 task/agent/cost/message/scheduler state，形成 source of truth。
  5. 缺少系统级容错模型：需要 distributed retry policy、idempotency guarantee、recovery orchestration、dead letter queue。
- 下一关键路线：先抽象 Event Layer，再引入 Scheduler，最后统一 State Store。
- 架构图任务：把现有系统重构成一张可论文/可工程实现的系统级架构图，包含 Event Bus + Scheduler + Unified State Store + Agent Workers + Audit/Policy Plane。

## 2026-06-20 14:35
- 主人提醒胖小必须坐回“指挥者”位置：CodeWhale / Codex / OpenCode / Xiaomi Mimo 都要参与执行，胖小负责拆任务、调度、验收和汇报，不默认亲自下场写代码。
- 系统级架构交付完成：
  - 新增 `docs/SYSTEM_ARCHITECTURE.md`：Control Plane / Event Layer / Scheduler / Unified State Store / Agent Workers / Policy & Audit Plane / Milestone A/B/C。
  - 新增 `docs/architecture/system_architecture.html`：可打开的系统级架构图。
  - `README.md` 与 `PROJECT_PLAN.md` 已链接上述交付物。
- 多智能体验证：
  - Codex 审查架构交付物，确认架构文档/图/链接基本通过；发现 `check_env.py` 依赖 openclaw PATH、PowerShell 不展开 `scripts/*.py`。
  - Xiaomi Mimo 复核并给出总体 PASS，建议 `check_env.py` 增加 `--skip-external`、README 补 Windows 友好验证命令。
  - CodeWhale 实现 `scripts/check_env.py --skip-external` 和 README 验证命令补充。
  - Codex 二审通过：默认行为不变，`--skip-external` 可跳过外部命令，README 代码块有效。
- 回归验证：`python scripts/check_env.py --skip-external` 通过；`python scripts/validate_task.py` 通过；Windows 友好 `py_compile` 命令由 CodeWhale/Codex 验证通过。
- 临时调度工单已清理，`.opencode-task.txt` 恢复，未提交临时 `.codewhale/.codex/.mimo` 任务文件。

## 2026-06-20 15:51
- Milestone A：Event Layer 骨架完成并提交。
- CodeWhale 实现 `scripts/event_log.py`，支持：
  - `append` / `list` / `replay --dry-run`
  - 事件写入 `logs/events/YYYY-MM-DD.jsonl`
  - 死信目录 `logs/dead_letter/`
  - eventId 格式 `EVT-YYYYMMDD-000001`
  - 事件字段：eventId/eventType/timestamp/source/correlationId/causationId/payload/policySnapshot/status
  - 标准库 only、跨进程锁、JSONL UTF-8
- Codex 初审 FAIL，发现 High：eventId 未按 UTC 日期每日重置。
- CodeWhale 返修：`.state` 增加 `date` 字段，使用 UTC 日期；同天递增、跨天重置到 `000001`；兼容旧 state；保持锁机制。
- 新增 `scripts/test_event_id_reset.py`，覆盖 5 个场景：冷启动、同天递增、跨天重置、旧 state 兼容、快速连续分配。验证 5/5 通过。
- Codex 复审关键验证通过；最终由胖小总控确认 daily reset 修复可接受。
- 验证通过：`python scripts/test_event_id_reset.py`、`python scripts/check_env.py --skip-external`、`python scripts/validate_task.py`、`py_compile`。
- 自动 snapshot 曾误提交临时任务单，已用 `0bb6cfe chore: remove temporary agent task files` 清理。
- 当前提交链：`7196052 feat: add event layer skeleton` → `49cb976 auto-snapshot: 2026-06-20 15:39` → `0bb6cfe chore: remove temporary agent task files`。工作区干净。

## 2026-06-20 17:14
- Milestone B：State Builder 完成并验收通过。
- CodeWhale 实现 `scripts/build_state.py`，支持从 `tasks/tasks.json`、`config/agents.json`、`config/policies.json`、`logs/messages/*.jsonl`、`logs/audit/*.jsonl`、`logs/events/*.jsonl` 重建统一状态。
- 新增正式产物：
  - `state/system_state.json`
  - `state/snapshots/*.json`
- CLI 支持：`python scripts/build_state.py`、`--json`、`--dry-run`、`--snapshot`、`--output PATH`。
- `README.md` 与 `PROJECT_PLAN.md` 已同步 State Builder 示例/进度。
- Codex 初审 FAIL：
  - High：`--json` stdout 混入摘要文本，不是纯 JSON。
  - Low：JSONL 坏行报错只显示文件名，缺少相对路径上下文。
- CodeWhale 第一轮返修：`--json` 去掉摘要/OK 文本；JSONL 错误改为相对路径 + 行号。
- Codex 复审再次 FAIL：Windows 下 `ensure_ascii=False` 导致 stdout 使用 GBK，本应 UTF-8 可解析的 JSON 输出不稳定。
- CodeWhale 第二轮返修：`--json` stdout 改用 `ensure_ascii=True`，保证 ASCII/UTF-8 稳定；写入 `state/system_state.json` 和 snapshots 仍保留 `ensure_ascii=False`。
- Codex 最终复审 PASS：`--json` 可按 bytes 捕获、`utf-8-sig` 解码并 `json.loads`；`--dry-run`、`--snapshot`、`--output`、`check_env.py --skip-external`、`validate_task.py`、`py_compile` 均通过；tasks/config/messages/audit/events 未被污染。
- 自动提交记录：`bd858a8 feat: add unified state builder`、`329deab auto-snapshot: 2026-06-20 16:39`、`2130147 fix: make state builder json output machine-readable`。
- 清理误提交临时审查任务单：`b187c95 chore: remove temporary state builder review task`。
- 当前状态：Milestone B 可接受，下一步进入 Milestone C：Scheduler Tick（dry-run 优先，不启动真实 ACP Agent）。

## 2026-06-20 17:58
- 主人要求继续 Agent Chat 系统化升级。
- 先验收已有 Milestone C Scheduler Tick：`scripts/scheduler_tick.py --dry-run` / `--json` / `--write-event --dry-run` 可运行；基础回归通过。
- 提交验证事件与状态快照：`1dd8cab test: record scheduler tick validation`。
- 增强 Milestone C 调度器规划能力，仍保持 dry-run only，不真实派工：
  - `build_state.py` 的 policy state 新增 `maxRetries`。
  - `scheduler_tick.py` 增加 maxRetries 检查、backpressure 警告、dead-letter 候选识别。
  - Agent 选择改为只按 `in_progress` 负载估算，避免 done/failed 历史任务污染调度。
  - `--json` 输出新增 `warnings`、`deadLetterCandidates`、`constraints.backpressure`。
- 构造状态测试通过：可调度、maxConcurrency 阻塞、无 enabled agent 阻塞、retry 超限 dead-letter、backpressure warning。
- 回归通过：`py_compile`、`build_state.py --snapshot`、`scheduler_tick.py --dry-run`、`scheduler_tick.py --dry-run --json`、`check_env.py --skip-external`、`validate_task.py`。
- 已提交：`b1f1b76 feat: enhance scheduler retry and backpressure planning`。


## 2026-06-20 18:29
- 主人纠偏：当前优先级切回“先做旧方案/原始方案”，不要继续默认沿系统级 Event Layer + Scheduler + State Store 升级线猛冲。
- 解释：前面 Milestone A/B/C 是把 MVP 往真正系统升级的路线，但主人现在明确要求先按旧方案推进。
- 后续工作原则：先回看 `G:\agent chat\方案.docx`、`方案2.docx`、`增补.doc`、`增补2.docx` 和已有审阅产物，重新抽出“旧方案优先落地清单”；胖小负责调度/验收，不默认亲自下场写代码。

## 2026-06-20 18:44
- 已按主人要求切回旧方案落地主线，不再继续默认推进系统级 Event/Scheduler/State 升级线。
- 根据旧方案增补文档的优先级，落地三块低风险管理能力：
  1. `scripts/snapshot_config.py`：配置快照/列表/查看/恢复，备份 config/tasks/state/关键文档；恢复前自动 pre-restore 备份；写审计。
  2. `scripts/record_cost.py` + `scripts/show_cost.py`：本地成本/Token 估算台账，替代旧方案不可确认的 `/usage` 第一版；只记录/估算/阈值提示，不自动暂停 Agent，不承诺精确账单。
  3. `scripts/command_map.py`：旧方案伪命令映射器，只读输出真实替代方式或 forbidden/pending，防止直接执行 `/usage`、`/acp spawn`、`/self-heal` 等高风险占位命令。
- README/PROJECT_PLAN 已同步“当前主线切回旧方案落地”。
- 清理了误提交的 `docs/_tmp_old_scheme_extract/*.txt` 临时抽取文件，并在 `.gitignore` 加入临时抽取目录、snapshots、logs/cost。
- 验证通过：全 scripts py_compile、snapshot list/show、record_cost dry-run、show_cost json、check_env.py --skip-external、validate_task.py。
- 下一步建议：旧方案“多维度告警雏形”，先做本地状态/日志告警，不做自动自愈；或者继续完善成本日报/CSV 导出。
