这三个部分已经全部实现了：

1. **`backend/weather_core.py`** — `WeatherClient` 类中已有 `get_7day_forecast` 方法（第138行），内部调用 `/weather/7d` API，返回 `date`、`temp_min`、`temp_max`、`text_day`、`text_night`、`wind_dir`、`wind_scale`、`humidity`。还有 `_fetch_7d`（第185行）和 mock 支持。模块级函数 `get_7day_forecast_with_fallback`（第255行）也已存在。

2. **`app.py`** — `/api/forecast` 端点已定义（第30-33行），调用 `get_7day_forecast_with_fallback`，响应模型为 `ForecastData`。

3. **`backend/schemas.py`** — `DailyForecast`（第30行）和 `ForecastData`（第41行）模型已定义，字段与 `weather_core.py` 的返回结构完全对应。

代码已经完整，无需修改。如果你需要调整字段或增加新功能，请告诉我。
