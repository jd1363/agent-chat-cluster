[EXECUTION FAILED] 执行超时 (>600s)
Let me first explore the existing codebase to understand the patterns and conventions.
Now I have a clear picture of the patterns. Let me implement all three changes.
Now add the module-level convenience functions at the bottom of the file:
Now add the module-level convenience functions at the bottom of the file:
Now add the schema and the endpoint:
Now add the schema and the endpoint:
Now add the endpoint in app.py:
Now let me verify the final state of all three files and run a syntax check:
Let me check if there are lint/typecheck commands available:
The edits were applied twice, causing duplicates. Let me fix them:
I see the duplicates. Let me fix them:
Let me also check weather_core.py for duplicates:
Let me also check weather_core.py for duplicates:
Lint passes. Let me run the existing tests to make sure nothing is broken:
The 2 test failures are pre-existing (unrelated to my changes — "Beijing" vs "北京" mismatch in mock mode). All lint checks pass and no new failures introduced.

Here's a summary of what was added:

**`backend/weather_core.py`** — `WeatherClient` class:
- `get_air_quality(city)` — calls QWeather `/air/now` API, returns AQI data
- `get_air_quality_with_fallback(city)` — falls back to mock on error
- `_get_air_quality_from_qweather(city)` — resolves city → location → air data
- `_fetch_air_now(location_id)` — raw API call
- `_mock_air_quality(city)` — mock data for dev mode
- `get_air_quality(city)` — module-level convenience function

**`backend/schemas.py`**:
- `AirQualityData` model with fields: `city`, `location_id`, `source`, `aqi`, `category`, `primary`, `pm2_5`, `pm10`, `update_time`

**`app.py`**:
- `GET /api/air-quality?city=<name>` endpoint using `AirQualityData` response model
