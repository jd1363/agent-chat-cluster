已完成。在 `scripts/text_utils.py` 中创建了 `slugify` 工具函数，满足以下要求：

- **纯标准库**：仅使用 `re` 模块
- **处理逻辑**：小写 → 空格/下划线转连字符 → 去除非字母数字及连字符外字符 → 合并多个连字符 → 去首尾连字符
- **类型注解与 docstring**：签名 `def slugify(text: str) -> str`，附带完整文档
- **自测块**：`if __name__ == "__main__"` 中测试了 6 个用例，覆盖常规空格、下划线、前后多余连字符、大写、连续符号等场景

运行结果：

```
[PASS] slugify('Hello World') -> 'hello-world' (expected 'hello-world')
[PASS] slugify('This is a test_string!') -> 'this-is-a-test-string' (expected 'this-is-a-test-string')
[PASS] slugify('---Multiple___Spaces and Symbols!!!---') -> 'multiple-spaces-and-symbols' (expected 'multiple-spaces-and-symbols')
[PASS] slugify('UPPER lower 123') -> 'upper-lower-123' (expected 'upper-lower-123')
[PASS] slugify('__leading_trailing__') -> 'leading-trailing' (expected 'leading-trailing')
[PASS] slugify('a---b___c') -> 'a-b-c' (expected 'a-b-c')

All tests passed.
```
