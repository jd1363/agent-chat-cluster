已完成。仅在 `app.py` 中做了以下修改：

1. **导入模块**：在顶部添加了 `import datetime` 和 `import time`。
2. **记录启动时间**：在 `app = FastAPI(...)` 之后添加了 `_start_time = time.time()`。
3. **新增路由**：在现有路由（`/api/ai-chat`）之后添加了 `GET /api/stats` 端点，返回：
   - `uptime_seconds`：服务运行秒数
   - `endpoints_count`：已注册路由数量
   - `api_version`：固定值 `"1.0"`
   - `timestamp`：当前 UTC 时间的 ISO 格式字符串


--- git status ---
M app.py

--- git diff --stat ---
app.py | 22 ++++++++++++++++++++++
 1 file changed, 22 insertions(+)