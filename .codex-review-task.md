# Review Task: Milestone B State Builder

You are Codex acting as reviewer. Do NOT modify files.

CodeWhale implemented Milestone B State Builder:
- `scripts/build_state.py`
- `state/system_state.json`
- `state/snapshots/*.json`
- README updates
- PROJECT_PLAN updates

Please perform a strict review and verification.

## Review focus

1. `scripts/build_state.py`
   - Standard library only.
   - Python 3.8/3.9 compatible: no `list[dict]`, `dict | None`, `str | None`, or other 3.10+ only syntax.
   - Reads required files:
     - `tasks/tasks.json`
     - `config/agents.json`
     - `config/policies.json`
   - Reads optional logs safely:
     - `logs/messages/*.jsonl`
     - `logs/audit/*.jsonl`
     - `logs/events/*.jsonl`
   - Ignores non-date event files such as `.state` / `.lock`.
   - Does NOT modify original business files: tasks/config/messages/audit/events.
   - Builds a coherent `state/system_state.json` with tasks/agents/messages/audit/events/policy/scheduler/sourceFiles.
   - `--dry-run` must not write system_state or snapshot.
   - `--snapshot` writes a timestamped snapshot.
   - `--output PATH` works.
   - JSON parse errors and JSONL bad lines fail clearly with file + line number.
   - Handles missing optional log dirs as empty.
   - Scheduler summary is reasonable: pendingTasks, inProgressTasks, availableAgents, backpressure, reasons.

2. Documentation
   - README examples are accurate.
   - PROJECT_PLAN wording says skeleton/state builder, not full scheduler.

3. Scope
   - Check git status/diff.
   - Ensure no unintended changes to `tasks/tasks.json`, config, logs/messages, logs/audit, logs/events.

## Verification commands

Run low-risk local commands only:

```bash
python scripts/build_state.py --dry-run
python scripts/build_state.py --json
python scripts/build_state.py --snapshot
python scripts/build_state.py --output state/system_state.json
python scripts/check_env.py --skip-external
python scripts/validate_task.py
python -c "import pathlib, py_compile; [py_compile.compile(str(p), doraise=True) for p in pathlib.Path('scripts').glob('*.py')]"
git status -s
git diff --stat
```

It is okay that build_state writes `state/system_state.json` and one snapshot.

## Output

Concise Chinese report:
- PASS/FAIL
- Findings by severity: Critical/High/Medium/Low
- Verification results
- Accept, request fixes, or proceed

Hard constraints:
- Do not modify files.
- Do not commit.
- Do not run network requests.
- Do not start real ACP agents.
